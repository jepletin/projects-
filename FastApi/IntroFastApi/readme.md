1.Creating your project directory FastApi then a folder inside it IntroFastApi  then pipenv shell
2.creating a virtual environment<pipenv shell>
3. install fastapi[standard] pipenv install "fastapi[standard]"
4.creating application code 
5. running the server pipenv run fastapi dev  app.py 